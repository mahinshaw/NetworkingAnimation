This is a project for my Networking class.  It is an animation that describes the how information travels on a network.

This is a Java project, coded with Eclipse.  This is also my first attempt using git.